import React from 'react'

import type { TrackResult } from '@/tracks'

import './track-form.styles.sass'

type TrackFormProps = {
  track: TrackResult
}

export const TrackForm: React.FC<TrackFormProps> = () => {
  return (
    <div className='track-form'>
    </div>
  )
}
