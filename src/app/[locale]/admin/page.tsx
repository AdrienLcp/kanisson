'use server'

import React from 'react'

const AdminPage: React.FC = async () => {
  // Get auth user & throw 404 if not admin

  return (
    <div className='page'>
      Admin page
    </div>
  )
}

export default AdminPage
